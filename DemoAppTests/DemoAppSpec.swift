import Quick
import Nimble
@testable import DemoApp

class DemoAppSpec: QuickSpec {
    override func spec() {
        describe("test") {
            it("should pass") {
                expect(true) == true
            }
        }
    }
}
